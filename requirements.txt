anthropic>=0.40.0
requests>=2.31.0
python-dotenv>=1.0.0
# Optional — only for non-Anthropic (OpenAI-compatible) providers:
openai>=1.40.0
# Optional web dashboard:
flask>=3.0.0
markdown>=3.5.0
# Tests (python -m pytest tests/):
pytest>=8.0.0
